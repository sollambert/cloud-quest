import './Room.css'
import {useSelector} from 'react-redux';

function Room() {

    // let room = useSelector(store => store.room);

    return (
        <p className="room">
            {/* {room.image} */}
        </p>
    )
}

export default Room;